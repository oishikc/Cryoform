# CryoForm — Python dependencies
# Install: pip install -r requirements.txt
#
# Note: RDKit is best installed via conda if pip fails:
#   conda install -c conda-forge rdkit

streamlit>=1.40.0
scikit-learn>=1.4.0
pandas>=2.0.0
numpy>=1.26.0
plotly>=5.18.0
joblib>=1.3.0

# RDKit — try pip first, fall back to conda
rdkit>=2023.9.1
